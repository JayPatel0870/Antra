namespace Exercise04
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //int startNum = 10;
            //int endNum = 50;

            //int[] primes = PrimeNumber.FindPrimeInRange(startNum, endNum);

            //Console.WriteLine("Prime numbers in range:");
            //foreach (var prime in primes)
            //{
            //    Console.Write(prime + " ");
            //}

            //string[] input = Console.ReadLine().Split();
            //int[] arr = Array.ConvertAll(input, int.Parse);
            //int k = int.Parse(Console.ReadLine());

            //int n = arr.Length;
            //int[] sum = new int[n];

            //for (int r = 1; r <= k; r++)
            //{
            //    // Call the rotateArray method from RotateArray class
            //    RotateArray.rotateArray(arr);

            //    Console.WriteLine($"rotated{r}[] = {string.Join(" ", arr)}");

            //    // Add current rotated array to sum array
            //    for (int i = 0; i < n; i++)
            //    {
            //        sum[i] += arr[i];
            //    }
            //}

            //Console.WriteLine($"sum[] = {string.Join(" ", sum)}");


            //string[] input = Console.ReadLine().Split();
            //int[] arr = Array.ConvertAll(input, int.Parse);

            //LongestString.FindLongestSequence(arr);

            //string input = Console.ReadLine();

            //// Check if input is empty
            //if (string.IsNullOrWhiteSpace(input))
            //{
            //    Console.WriteLine("Input cannot be empty. Please enter a sequence of numbers.");
            //    return;  // Exit the program if input is empty
            //}

            //string[] inputArray = input.Split();

            //List<int> numbers = new List<int>();

            //// Try to parse each element and add it to the numbers list
            //foreach (var str in inputArray)
            //{
            //    if (int.TryParse(str, out int num))
            //    {
            //        numbers.Add(num);
            //    }
            //    else
            //    {
            //        Console.WriteLine($"'{str}' is not a valid integer. Please enter valid integers.");
            //        return;  // Exit the program if any value is invalid
            //    }
            //}

            //// Call the method to find the most frequent number
            //Frequency.FindMostFrequentNumber(numbers.ToArray());
        }
    }
}
